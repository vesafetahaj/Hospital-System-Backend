@using Hospital_System_Management.Models

@{
    Layout = "~/Views/Shared/_ReceptionistLayout.cshtml";
}
@model List<RegisterModel>

<div id="main">
    <h1>Registered patients </h1>
    <br />

    @if (Model.Count > 0)
    {
        <table class="table table-hover">
            <thead style="background-color: #9abddc;">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Age</th>
                    <th>Place of birth</th>
                    <th>Date of birth</th>
                    <th>ID Card</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody style="background-color: #EAF0FA;">
                @foreach (var register in Model)
                {
                    <tr>
                        <td>@register.Name</td>
                        <td>@register.Surname</td>
                        <td>@register.Age</td>
                        <td>@register.BirthPlace</td>
                        <td>@register.DateSubmitted</td>
                        <td>@register.IDCard</td>
                        <td>
                            <a href="@Url.Action("EditRegistration", "Receptionist", new { id = register.Id })" class="btn btn-primary btn-sm">Edit</a>
                            <a href="@Url.Action("DeleteRegistration", "Receptionist", new { id = register.Id })" class="btn btn-danger btn-sm">Delete</a>

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No patients found.</p>
    }
</div>
